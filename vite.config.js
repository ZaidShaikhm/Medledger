import { defineConfig } from 'vite'

export default defineConfig({
  // index.html is already at the root — no extra config needed.
  // Add server options here if you need a custom port:
  // server: { port: 3000 }
})
